[build]
  base    = ""                     # empty = repo root (change if project is in subfolder)
  command = "npm run build"
  publish = "dist"                 # ← adjust if your build outputs elsewhere (e.g. dist/public)

[build.environment]
  NODE_VERSION = "20"              # or "22"